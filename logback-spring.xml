<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true" scan="true">
    <statusListener class="ch.qos.logback.core.status.OnConsoleStatusListener"/>
    <property name="LOG_DIR" value="./logs/"/>
    <property name="FILE_NAME" value="server-configuration"/>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--<pattern>%highlight(%-6level %d{ISO8601} [%(%t)] %(%C{1.}): %msg%n%throwable)</pattern>-->
            <pattern>%highlight([%-5level] %d{"ISO8601"} [${HOSTNAME}][${server.name}][%contextName][%class.%method][thread=%thread][requestId:%X{RequestId:--}][severity:%level]%msg%n%throwable)</pattern>
        </encoder>
    </appender>
    <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/${FILE_NAME}/log_%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <!--<pattern>%-5level %d{ISO8601} [%thread] %logger{36} - %msg%n</pattern>-->
            <pattern>%-5level %d{ISO8601} [${HOSTNAME}][${server.name}][%contextName][%class.%method][%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <logger name="com.project.springboot.app.config" additivity="true">
        <level value="TRACE" />
    </logger>
    <root level="info">
        <appender-ref ref="file" />
        <appender-ref ref="console"/>
    </root>
</configuration>
